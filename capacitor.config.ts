import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.learningdevelopment.org',
  appName: 'learninganddevelopement',
  webDir: 'build'
};

export default config;
